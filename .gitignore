__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# 官方依赖使用独立 Git 仓库管理，不纳入本项目版本控制。
dependencies/

# 仿真验证生成的报告和相机图像。
artifacts/

# 正式采集和转换的数据集通常体积很大，不纳入代码仓库。
datasets/

# 训练 checkpoint 和策略服务日志保留在本机或对象存储，不纳入 Git 历史。
checkpoints/
logs/

# 文档附件中的安装程序应从官网下载，避免把大型二进制发行包提交到仓库。
*.exe
